[tool.uv.workspace]
members = ["cli", "common", "rag", "server"]

[tool.uv.sources]
cli = { workspace = true }
common = { workspace = true }
rag = { workspace = true }
server = { workspace = true }

[project]
name = "localllm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cli",
    "common",
    "rag",
    "server",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
testpaths = ["cli/tests", "server/tests", "rag/tests"]
