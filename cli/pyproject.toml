[project]
name = "cli"
version = "0.1.0"
description = "CLI for interacting with a local LLM"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["cli"]
